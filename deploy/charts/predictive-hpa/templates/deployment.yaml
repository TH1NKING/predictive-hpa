apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "predictive-hpa.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "predictive-hpa.labels" . | nindent 4 }}
spec:
  # Lease 选主保证单个活跃 manager；新 leader 重建观测与缩容保护。
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "predictive-hpa.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "predictive-hpa.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "predictive-hpa.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: manager
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --leader-elect
            # 核验当前 CPU 观测后，在进程内积累 EWMA 历史。
            - --prometheus-url={{ .Values.prometheus.url }}
            {{- if .Values.metricsService.enabled }}
            - --metrics-bind-address=:{{ .Values.metricsService.port }}
            {{- end }}
          {{- if .Values.metricsService.enabled }}
          ports:
            - name: metrics
              containerPort: {{ .Values.metricsService.port }}
              protocol: TCP
          {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
