# 本文件由 hack/sync-chart-crd.sh 从 config/crd/bases/ 同步生成，请勿手改。
# source of truth: config/crd/bases/autoscaling.brian.io_predictivehpas.yaml
# 重新同步：make manifests && ./hack/sync-chart-crd.sh
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: predictivehpas.autoscaling.brian.io
spec:
  group: autoscaling.brian.io
  names:
    kind: PredictiveHPA
    listKind: PredictiveHPAList
    plural: predictivehpas
    shortNames:
    - phpa
    singular: predictivehpa
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scaleTargetRef.name
      name: Reference
      type: string
    - jsonPath: .spec.minReplicas
      name: MinPods
      type: integer
    - jsonPath: .spec.maxReplicas
      name: MaxPods
      type: integer
    - jsonPath: .status.currentReplicas
      name: Replicas
      type: integer
    - jsonPath: .status.currentCPUUtilizationPercentage
      name: Current%
      type: integer
    - jsonPath: .status.predictedCPUUtilizationPercentage
      name: Predicted%
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PredictiveHPA is the Schema for the predictivehpas API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PredictiveHPA
            properties:
              maxReplicas:
                description: |-
                  maxReplicas is the upper bound for the number of replicas.
                  Must be >= minReplicas.
                format: int32
                minimum: 1
                type: integer
              minReplicas:
                description: |-
                  minReplicas is the lower bound for the number of replicas.
                  Defaults to 1 when unset.
                format: int32
                minimum: 0
                type: integer
              prediction:
                description: prediction configures the time-series prediction used
                  to drive proactive scaling.
                properties:
                  algorithm:
                    default: EWMA
                    description: algorithm selects the prediction algorithm. Currently
                      only "EWMA" is supported.
                    enum:
                    - EWMA
                    type: string
                  alphaPercent:
                    description: |-
                      alphaPercent is the EWMA smoothing factor scaled by 100.
                      A higher value gives more weight to recent observations.
                      For example, 30 means alpha = 0.3.
                    format: int32
                    maximum: 99
                    minimum: 1
                    type: integer
                  horizon:
                    description: |-
                      horizon is how far into the future the controller predicts metric values
                      to drive the scaling decision.
                      Examples: "30s", "1m".
                    type: string
                  window:
                    description: |-
                      window is the lookback duration of historical samples used to compute the EWMA.
                      Samples older than this window are dropped.
                      Examples: "5m", "10m".
                    type: string
                required:
                - algorithm
                - alphaPercent
                - horizon
                - window
                type: object
              scaleDownStabilizationWindowSeconds:
                default: 60
                description: |-
                  scaleDownStabilizationWindowSeconds is how long the controller waits before
                  applying a scale-down decision, to prevent oscillation. Defaults to 60s,
                  which is more aggressive than the native HPA default (300s) since the
                  EWMA already smooths out short-term spikes.
                format: int32
                minimum: 0
                type: integer
              scaleTargetRef:
                description: |-
                  scaleTargetRef points to the target resource to scale (e.g. a Deployment).
                  The reference is resolved within the same namespace as the PredictiveHPA.
                properties:
                  apiVersion:
                    description: apiVersion is the API version of the referent
                    type: string
                  kind:
                    description: 'kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                required:
                - kind
                - name
                type: object
              targetCPUUtilizationPercentage:
                description: |-
                  targetCPUUtilizationPercentage is the desired average CPU utilization
                  across all pods of the scale target, expressed as a percentage of the
                  per-pod CPU request.
                format: int32
                maximum: 100
                minimum: 1
                type: integer
            required:
            - maxReplicas
            - prediction
            - scaleTargetRef
            - targetCPUUtilizationPercentage
            type: object
          status:
            description: status defines the observed state of PredictiveHPA
            properties:
              conditions:
                description: conditions represent the current state of the PredictiveHPA
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentCPUUtilizationPercentage:
                description: |-
                  currentCPUUtilizationPercentage is the most recently observed average
                  CPU utilization across pods of the scale target.
                format: int32
                type: integer
              currentReplicas:
                description: currentReplicas is the actual number of replicas observed
                  on the scale target.
                format: int32
                type: integer
              desiredReplicas:
                description: desiredReplicas is the replica count last computed by
                  the controller.
                format: int32
                type: integer
              lastScaleTime:
                description: lastScaleTime is the last time the controller adjusted
                  the replica count.
                format: date-time
                type: string
              predictedCPUUtilizationPercentage:
                description: |-
                  predictedCPUUtilizationPercentage is the EWMA-predicted CPU utilization
                  at horizon, used to drive the scale-up decision.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
